# from django.utils.decorators import decorator_from_middleware
# from middleware import HighwayMiddleware

# highway = decorator_from_middleware(HighwayMiddleware)
# highway.__doc__ = "Decorator for views that gzips pages if the client supports it."